<h1>All the Animals</h1>
<%= link_to 'Add Animal', new_animal_path %>
<% @animals.each do |animal| %>
    <%= animal.name %>
    <%= link_to 'Show Animal', animal_path(animal.id) %>
    <%= link_to 'Delete', animal_path(animal.id), method: :delete %>
    <% end %>

